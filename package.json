{
  "name": "curio-platform",
  "version": "1.0.0",
  "description": "Root workspace for Curio Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
